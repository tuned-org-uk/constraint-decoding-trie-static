[package]
name = "constraint-decoding-trie"
version = "0.1.0"
edition = "2024"

[dependencies]
rayon = "1.11.0"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }
rand = { version = "0.10" }

[[bench]]
name = "vntk_benches"
harness = false
